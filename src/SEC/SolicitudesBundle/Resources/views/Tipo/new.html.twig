{% extends '::base.html.twig' %}

{% block body %}
    <div class="clear"></div>

    <article class="module width_full">

<h1>Tipo creation</h1>

<form action="{{ path('tipo_create') }}" method="post" {{ form_enctype(form) }}>
    {{ form_widget(form) }}
    <p>
        <button type="submit">Create</button>
    </p>
</form>

<ul class="record_actions">
    <li>
        <a href="{{ path('tipo') }}">
            Back to the list
        </a>
    </li>
</ul>

    </article>
{% endblock %}